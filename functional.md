# 剪切板（ShearPlate）功能说明

> 本文档基于对当前代码（版本 0.1.11）的完整审阅编写，描述项目实际实现的功能、架构与模块职责。

---

## 1. 项目概述

**剪切板（ShearPlate）** 是一款基于 **C++17 + Qt 6** 的 Windows 剪贴板历史管理工具。程序常驻系统右下角托盘，实时监听并记录用户复制/剪切的 **文本、图片、文件（文件夹）**，以磁盘文件形式持久化保存。用户可通过托盘图标或全局快捷键呼出悬浮面板，对历史记录进行单击上屏、拖拽导出、打开链接/所在目录、置顶、删除等操作。

**核心特点：纯本地运行**，数据只保存在用户本机，不上传任何内容。

- 技术栈：C++17、Qt 6.x（Core / Gui / Widgets / Network）
- 构建：CMake 3.16+，Ninja 或 MinGW Makefiles
- 平台：Windows（依赖 `SendInput`、`RegisterHotKey`、`QLocalServer`、Shell COM 等系统能力）

---

## 2. 总体架构与模块划分

应用采用 `ClipboardApp` 作为总控（Facade），串联各功能模块。模块职责如下：

| 文件 | 职责 |
| --- | --- |
| `main.cpp` | 程序入口，设置组织名/应用名/版本，禁用「关窗即退出」，启动单实例检查 |
| `ClipboardApp` | 应用总控：模块串联、单实例、去重、文件失效检查、悬置粘贴状态机、设置分发 |
| `ClipboardManager` | 剪贴板监听/捕获/写入；敏感内容过滤；自身写入抑制 |
| `ClipboardStore` | 磁盘 JSON 持久化 + 过期清理 |
| `ClipboardItem` | 记录数据结构：类型、分类、链接/截图判定、去重、预览文案 |
| `ClipboardListModel` | 列表模型：排序（置顶优先 + 时间倒序）、分类过滤、拖拽 MIME 数据 |
| `ClipboardItemDelegate` | 条目自绘：卡片、图标、缩略图、图钉、多文件布局 |
| `ClipboardListView` | 列表视图：拖拽、悬停大图预览、「打开」图标点击 |
| `ClipboardPanel` | 圆角悬浮面板：滑入滑出动画、分类栏、右键菜单、空状态 |
| `TrayController` | 系统托盘图标与右键菜单 |
| `GlobalHotkey` | 全局快捷键（`RegisterHotKey`） |
| `SettingsDialog` | 设置面板（常规/外观/快捷键/关于） |
| `AppSettings` | 设置读写（`QSettings`，含开机自启写注册表） |
| `Theme` | 浅色/深色主题配色与应用 |
| `HoverIconButton` | 悬停切换图标的按钮 |
| `Util` | 图标/图片加载（支持外部替换 + 主题图标） |
| `WinUtil` | Win32 封装：前台窗口判断、模拟 Ctrl+V、激活窗口、毛玻璃 |

---

## 3. 核心功能详述

### 3.1 实时监听与持久存储

- 通过 `QClipboard::dataChanged` 实时监听系统剪贴板变化。
- 自动捕获三种类型：
  - **文本**：`CF_UNICODETEXT`，存为 `*.json`
  - **图片**：存为 `*.json`（元数据）+ `*.png`（图片本体）
  - **文件/文件夹**：`CF_HDROP` 路径列表，存为 `*.json`
- 每条记录落盘为独立文件，位于 `%APPDATA%\ShearPlate\ShearPlate\clips\`。
- **去重**：
  - 捕获侧 1.5 秒内相同内容（MD5）视为系统重复触发，忽略；
  - 记录侧永久去重：与已有条目内容一致时不新增，而是刷新其时间戳并置顶。
- **过期清理**：默认保留 12 小时；启动时立即清理一次，之后每 60 秒轮询清理一次（保留时长可在设置中调整，1~720 小时）。

### 3.2 悬浮面板

- 无边框、圆角、置顶（`FramelessWindowHint | Tool | WindowStaysOnTopHint`），透明背景自绘圆角。
- 面板尺寸 330×529，位于屏幕右下方。
- 左键托盘图标 / 全局快捷键呼出，从屏幕右侧**滑入**（220ms）；关闭时**滑出**（200ms）。
- `WA_ShowWithoutActivating`：显示时不抢焦点，保持原窗口光标。
- 结构：顶部标题栏（Logo + 标题 + 计数 + 关闭按钮）→ 左侧分类栏 + 列表 → 空状态视图。
- **空状态**：无记录时居中显示提示图与「剪切板无内容」。

### 3.3 交互方式

| 操作 | 效果 |
| --- | --- |
| **单击条目** | 上屏（粘贴到当前光标处） |
| **Shift + 单击** | 剪切语义：粘贴后删除该历史条目（不删源文件） |
| **拖拽条目** | 左键拖动=复制；Shift+左键拖动=剪切（移动），拖到目标应用/资源管理器 |
| **右键条目** | 弹出菜单：置顶/取消置顶、删除 |
| **右键托盘图标** | 打开剪切板 / 设置 / 退出 |
| **点击文字末尾「打开」图标** | 打开网页链接 / 本地路径 / 所在目录 |

### 3.4 上屏（粘贴）——悬置粘贴状态机

点击条目并非简单 `Ctrl+V`，而是实现了一套「等待焦点」状态机：

1. 点击条目 → 将内容写入系统剪贴板（带自身抑制标志与敏感标记）。
2. **焦点可直接粘贴**（前台窗口有效、非本程序、非桌面/任务栏、有焦点控件）→ 立即 `SendInput` 模拟 `Ctrl+V`，结束。
3. **无有效焦点**（或焦点在本程序面板）→ 进入 `WaitingForFocus` 状态：
   - 记录目标窗口（当前前台窗口；若无效则取最近缓存的有效窗口）；
   - 通过 `SetWinEventHook(EVENT_OBJECT_FOCUS)` 监听焦点变化，仅当**目标窗口**重新获得焦点时自动粘贴；
   - 30 秒超时自动取消；
   - 通过 `AddClipboardFormatListener` + `WM_CLIPBOARDUPDATE` 监听剪贴板覆盖：若内容被第三方覆盖则取消悬置（不粘贴、不删除）。
4. 面板隐藏、点击另一条目、超时均会取消悬置。

该机制避免「面板抢焦点导致粘贴到错误窗口」，也避免任意窗口抢焦点触发误粘贴。

### 3.5 智能类型识别

- **文本**：
  - 自动识别网页链接（`http://` / `https://` / `www.`）与本地绝对路径；
  - 链接条目文字末尾显示「打开」图标，点击打开浏览器 / 资源管理器。
- **图片**：
  - 显示缩略图；尺寸接近屏幕分辨率（≥80%）判定为**截图**，文字显示「截图」；
  - 鼠标悬停在缩略图上时，在鼠标旁显示 320×240 大图预览。
- **文件**：
  - 显示系统关联图标；混合类型显示 `filelist.png` 图标；
  - 支持分类：视频、音频、图片、可执行文件、文档、压缩文件、文件夹；
  - 多文件智能汇总：两个视频 → 「共2个视频」，类型混合 → 「共2个文件」，下方展示文件名列表；
  - 单文件「打开」图标 → `explorer /select` 预选；多文件 → PowerShell + Shell COM 打开目录并全选这些文件。

### 3.6 置顶功能

- 条目支持「置顶 / 取消置顶」（右键菜单）。
- `isPinned` 字段持久化到 JSON。
- 置顶条目始终排在列表最前（非置顶条目按时间倒序），卡片使用独立底色/边框配色，右上角显示图钉图标（随置顶状态与主题切换）。

### 3.7 分类过滤

面板左侧分类栏提供 4 个过滤按钮：**所有 / 文件 / 图片 / 文本**（图标分默认态与选中态，随主题切换）。

### 3.8 敏感内容过滤

- 注册系统剪贴板格式 `ExcludeClipboardContentFromMonitorProcessing`、`CanIncludeInClipboardHistory`、`CanUploadToCloudClipboard`（Win10 1809+；旧系统自然降级）。
- **记录侧**：检测到敏感标记（被排除出系统剪贴板历史/云剪贴板）时不记录。
- **写入侧**：本程序上屏写入时附加「禁止进入系统剪贴板历史 / 禁止上传云剪贴板」标记，避免污染系统剪贴板历史与云剪贴板。
- 可独立开关；检测与写入均做了**防死锁**处理（`OpenClipboard` 失败即放弃判断，绝不重试）。

### 3.9 全局快捷键

- 默认 `Ctrl+Shift+V` 呼出/隐藏面板，即使程序不在前台也能触发。
- 基于 `RegisterHotKey` + `WM_HOTKEY`，支持字母、数字、F1~F24、空格、方向键等；可在设置中自定义。

### 3.10 单实例保护

- 通过 `QLocalServer`（名称 `ShearPlate-SingleInstance`）实现单实例。
- 重复启动时：新实例连接已有实例并发送 `activate`，随后退出；已有实例收到后自动呼出面板。

### 3.11 文件失效检查

- 面板隐藏时每 10 秒轮询一次：若文件条目的源文件已不存在（被删除/移动），自动移除该历史条目，避免粘贴无效路径。

### 3.12 主题与外观

- 三种主题：**跟随系统 / 浅色 / 深色**。
- 图标分 `light` / `dark` 两套，主题切换时自动重载（托盘图标、分类按钮、文本/链接/图钉图标等）。
- 跟随系统深浅色变化实时刷新。
- **毛玻璃（半透明）**：可在设置中开关。当前实现中系统级 acrylic/blur 已禁用（为保证无边框透明窗口圆角干净、无黑色填充），仅保留 paintEvent 的半透明背景效果。

---

## 4. 设置项（SettingsDialog）

设置面板分四页：**常规 / 外观 / 快捷键 / 关于**，控件变化即自动写入 `QSettings` 并通过 `settingsChanged` 信号即时刷新应用。

| 设置项 | 默认值 | 说明 |
| --- | --- | --- |
| 保留时长（小时） | 12 | 1~720，控制过期清理 |
| 开机自动启动 | 关 | 写注册表 `HKCU\...\Run` 键 |
| 点击内容时置顶并刷新保留时长 | 开 | 见下方「待观察问题」 |
| 文本类型显示图标 | 开 | 文本条目是否显示类型图标 |
| 启用敏感内容过滤 | 开 | 见 3.8 |
| 主题 | 跟随系统 | 跟随系统 / 浅色 / 深色 |
| 毛玻璃（半透明）效果 | 开 | 见 3.12 |
| 全局快捷呼出键 | Ctrl+Shift+V | 自定义组合键 |
| 关于页 | - | 作者、联系 QQ、版本号 |

---

## 5. 数据存储位置

- 剪贴板记录：`%APPDATA%\ShearPlate\ShearPlate\clips\`
  - 文本：`<id>.json`
  - 图片：`<id>.json` + `<id>.png`
  - 文件：`<id>.json`
- 应用设置：注册表 `HKCU\Software\ShearPlate`
- 开机自启：注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 的 `ShearPlate` 键

---

## 6. 构建与分发

- `build.bat`：一键编译脚本，自动定位 Qt、MinGW、CMake/Ninja，编译并调用 `windeployqt` 部署 Qt 运行库，产出绿色免安装 `build\ShearPlate.exe`。
- `installer.iss`：Inno Setup 6 安装脚本，产出 `Output\ShearPlate-Setup.exe`（含开始菜单/桌面快捷方式与卸载入口）。
- `app.rc.in`：将 `favicon.ico` 注入 exe，并写入版本信息。
- `resources.qrc`：内嵌图标资源（light/dark 两套 + 通用图标），同时支持运行时从 exe 同目录读取外部图标以便替换。

---

## 7. 待观察问题（代码审阅发现）

以下是审阅中发现的、与文档/设计存在偏差或未完全接通之处（供后续维护参考）：

1. **「点击内容时置顶并刷新保留时长」设置项未生效**：`AppSettings::clickToTop()` 只在设置面板读写，`onPasteRequested`（单击上屏）中并未消费该设置，也不会刷新时间戳或置顶。当前唯一调用 `refreshTimestamp` 的地方是 `onClipCaptured` 的去重分支（固定行为，与开关无关）。README 中「单击置顶 + 刷新时效」的描述与实际代码不符。
2. **`TrayController::beforePanelShow` 信号为悬空信号**：在 `showPanel()` 中发出，但 `ClipboardApp` 未连接任何槽（旧版「缓存前台窗口」逻辑的遗留）。
3. **`ClipboardManager::paste()` / `copyToClipboard()` 未被调用**：实际单击上屏走 `ClipboardApp::onPasteRequested → writeToClipboard + simulateCtrlV`，这两个公有槽目前是未使用的能力入口。
4. **`shortcut_default.png` / `shortcut_active.png` 图标未在代码中使用**：已注册进 `resources.qrc` 与 README，但当前「打开」图标实际使用 `link.png`，该两个图标为遗留资源。
5. **README 中的 `ToastWidget` 模块已删除**：`REFACTOR_PLAN.md` 记录其已移除（需求要求移除所有用户可见提示），当前代码已无该文件，README 项目结构一节存在过时描述。
6. **系统级毛玻璃实际被禁用**：`WinUtil::enableAcrylic` 目前为空实现（注释说明为避免圆角黑色填充），设置中的「毛玻璃」实际只对应 paintEvent 的半透明背景，而非系统 acrylic/blur 效果。

---

## 8. 功能清单速览

- ✅ 实时监听文本 / 图片 / 文件，磁盘持久化
- ✅ 默认 12 小时过期自动清理（可调）
- ✅ 悬浮面板滑入/滑出，圆角无边框置顶
- ✅ 单击上屏（含 Shift 剪切语义）、拖拽复制/移动
- ✅ 悬置粘贴状态机（等待焦点自动粘贴、防覆盖、30s 超时）
- ✅ 智能类型识别（链接/路径/截图/文件分类/多文件汇总）
- ✅ 打开网页链接 / 本地路径 / 所在目录（单文件预选、多文件全选）
- ✅ 置顶条目（持久化 + 排序 + 外观 + 图钉）
- ✅ 分类过滤（所有/文件/图片/文本）
- ✅ 敏感内容过滤（记录侧 + 写入侧，防死锁）
- ✅ 全局快捷键（可自定义）
- ✅ 单实例保护
- ✅ 文件失效自动清理
- ✅ 深色/浅色/跟随系统主题
- ✅ 托盘图标与右键菜单
- ✅ 设置面板（常规/外观/快捷键/关于）
- ✅ 一键编译脚本与安装包脚本
